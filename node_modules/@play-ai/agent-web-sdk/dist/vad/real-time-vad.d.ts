import { type SpeechProbabilities } from './models';
interface RealTimeVADOptions {
    stream: MediaStream;
    onFrameProcessed: (probabilities: SpeechProbabilities, audio: Float32Array) => void;
}
export declare class MicVAD {
    private options;
    private audioContext?;
    private frameProcessor?;
    private audioResampler?;
    private initialized;
    static new(options: RealTimeVADOptions): Promise<MicVAD>;
    constructor(options: RealTimeVADOptions);
    init: () => Promise<void>;
    onAudioFrame: (frame: Float32Array) => Promise<void>;
    start: () => void;
    destroy: () => void;
}
export {};
